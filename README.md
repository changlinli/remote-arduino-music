Remote Arduino Music
====================

A short little program to control `pianobar` from an Arduino.

Install
-------

+ Rename `config.hs.default` to `config.hs`
+ Go to `config.hs` and change the FIFO as needed
+ Run `make`
